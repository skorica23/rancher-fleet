apiVersion: fleet.cattle.io/v1alpha1
kind: Bundle
metadata:
  name: awx-operator
spec:

  defaultNamespace: awx-operator
  targetNamespace: awx-operator

  helm:
    chart: awx-operator
    repo: https://github.com/ansible-community/awx-operator-helm/tree/main/charts/awx-operator #ansible-community.github.io/awx-operator-helm/
    version: "3.2.0"
    releaseName: awx-operator-test
    namespace: awx-operator
    installCrds: true
    takeOwnership: true

  targets:
    - {}
