defaultNamespace: awx

# No helm section needed — we deploy plain Kubernetes YAML
targets:
  - clusterSelector: {}

# Tell Fleet to apply Kubernetes manifests from this directory
manifests:
  - "./"
